###
# ```{rubric} BGPPeer Test Suite
# ```
# ---
# Tests to validate the deployment of the described BGPPeer.
suite: test suite for BGPPeer
release:
  name: calico
templates:
  - templates/bgppeer.yaml
values:
  - ../values.yaml

tests:
  - asserts:
      - isKind:
          of: BGPPeer
      - equal:
          path: metadata.name
          value: calico-global-bgp-peer
    it: should be a BGPPeer
    template: templates/bgppeer.yaml
    documentSelector:
      path: kind
      value: BGPPeer

